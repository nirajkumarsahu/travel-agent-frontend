import theme from "styles/theme";

// TODO - Need to update this once global changes are done
const headingConfig = {
  h1: {
    fontSize: `${theme.fs_medium_larger}px` // 24px
  },
  h2: {
    fontSize: `${theme.fs_medium}px` // 20px
  },
  h3: {
    fontSize: `${theme.fs_primary}px` // 18px
  },
  h4: {
    fontSize: `${theme.fs_secondary}px` // 16px
  },
  h5: {
    fontSize: `${theme.fs_small}px` // 14px
  },
  h6: {
    fontSize: `${theme.fs_xsmall}px` // 12px
  }
};

export default headingConfig;
